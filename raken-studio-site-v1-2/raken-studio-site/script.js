document.querySelectorAll('.chip-row').forEach(row=>{const video=document.getElementById(row.dataset.target);row.querySelectorAll('.chip').forEach(btn=>{btn.addEventListener('click',()=>{row.querySelectorAll('.chip').forEach(b=>b.classList.remove('active'));btn.classList.add('active');video.pause();video.poster=btn.dataset.poster||'';video.src=btn.dataset.video;video.load();video.play().catch(()=>{});});});});
